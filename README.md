# 2027秋招看板

这是面向手机和电脑的静态求职看板。页面打开后会从 `jobs.json` 拉取最新岗位数据；个人投递状态、备注和回执只保存在使用者的浏览器中。

发布内容不包含联系方式、DOCX简历或原始实验报告。

## 更新岗位

在主项目中修改 `autumn2027/jobs.json` 后运行 `output/autumn2027/job-dashboard/build-data.mjs`，再提交本目录生成的变更。网站会在刷新后读取最新数据。
